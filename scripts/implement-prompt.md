You are implementing a feature for the discord-claude-app-builder bot based on competitive research.

## Research Findings

{{RESEARCH_SUMMARY}}

## Instructions

1. **Read the codebase first.** Understand the project structure, existing patterns, and conventions before writing code. Key files to check:
   - `bot.py` — main bot setup
   - `commands/` — slash command definitions
   - `handlers/` — command handler logic
   - `views/` — Discord UI views
   - `helpers/` — utility functions

2. **Create a feature branch:**
   ```
   git checkout -b {{BRANCH_NAME}}
   ```

3. **Implement the feature.** Follow existing patterns:
   - Commands go in `commands/` with handlers in `handlers/`
   - Use Discord.py interactions (slash commands, modals, buttons)
   - Keep changes minimal and focused — one feature per PR

4. **Commit your changes** with a clear commit message.

5. **Create a PR** using `gh pr create`:
   ```
   gh pr create --title "feat: <feature name>" --body "$(cat <<'EOF'
   ## Research
   <Summary of competitive intelligence that inspired this>

   ## Changes
   <Bullet list of what was added/modified>

   ## Competitors
   <Which competitors inspired this and what they do>

   ---
   *Auto-generated by nightly competitive research script*
   EOF
   )"
   ```

## Rules

- Do NOT modify existing features or break current functionality
- Do NOT push to main — always use a feature branch
- Do NOT create markdown files or documentation — put everything in the PR description
- Keep the implementation simple and self-contained
- If the feature requires new dependencies, add them to requirements.txt
